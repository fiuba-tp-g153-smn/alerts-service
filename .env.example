# Alerts Service Configuration

# Application
APP_HOST_PORT=6007
APP_ENV=development
LOG_LEVEL=INFO

# Optional: Override default IGN endpoints
# COUNTRY_GEOJSON_URL=https://wms.ign.gob.ar/geoserver/ows?service=WFS&version=1.0.0&request=GetFeature&typeName=ign:pais&outputFormat=application/json
# DEPARTMENTS_GEOJSON_URL=https://wms.ign.gob.ar/geoserver/ows?service=WFS&version=1.0.0&request=GetFeature&typeName=ign:departamento&outputFormat=application/json
# PROVINCES_GEOJSON_URL=https://wms.ign.gob.ar/geoserver/ows?service=WFS&version=1.0.0&request=GetFeature&typeName=ign:provincia&outputFormat=application/json

# S3 / object storage (for geojson backup and restore)
S3_ENDPOINT=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET_NAME=
S3_SECURE=true

# Path to the settings JSON file
SETTINGS_FILE=/config/settings.json

# MySQL Database
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_DATABASE=aviso_gempak
MYSQL_ROOT_HOST=localhost
MYSQL_ROOT_PASSWORD=REPLACE_WITH_SECURE_PASSWORD

MYSQL_USER=alerts_service
MYSQL_PASSWORD=REPLACE_WITH_SECURE_PASSWORD

MYSQL_READONLY_USER=avisos
MYSQL_READONLY_PASSWORD=REPLACE_WITH_SECURE_PASSWORD
MYSQL_READONLY_MAX_CONNECTIONS=50
MYSQL_READONLY_MAX_CONNECTIONS_PER_HOUR=10000

# Database schema management
# ---------------------------
# MANAGE_DB_SCHEMAS gates ALL Alembic migrations (centralized in alembic/env.py).
# Leave it unset in production: the schema is owned by the client/DBA, so
# `alembic upgrade head` becomes a total no-op and no DDL/SP/trigger ever runs
# (including against the client's external MySQL server, which can point at a
# different host/db/user).
#
# In dev/test set MANAGE_DB_SCHEMAS=true. There all migrations run, and the
# "external" taviso lives in the SAME database (aviso_gempak) — Alembic creates the
# `taviso` table there (no separate schema or special grants needed), and the
# existing read-only user (avisos) serves the read connection.
MANAGE_DB_SCHEMAS=true

# External taviso Database (read-only)
MYSQL_TAVISO_HOST=mysql
MYSQL_TAVISO_PORT=3306
MYSQL_TAVISO_DATABASE=aviso_gempak
MYSQL_TAVISO_USER=avisos
MYSQL_TAVISO_PASSWORD=REPLACE_WITH_SECURE_PASSWORD

# Alert Generation
OUTPUT_DIR=/app/output/alerts
ALERT_CACHE_DIR=/app/cache
